package me.caneva20.CNVCore.Listeners;

import me.caneva20.CNVCore.CNVCore;
import org.bukkit.event.Listener;

@SuppressWarnings({"FieldCanBeLocal", "unused"})
public class ItemDespawnListener implements Listener {
    private CNVCore plugin;

    public ItemDespawnListener(CNVCore plugin) {
        this.plugin = plugin;
    }
}
